/*
use crate::sensor::Sensor;

trait Barometer: Sensor {
    fn get_sea_level_pressure(&self) -> f32;
    fn get_sea_level_temperature(&self) -> f32;
    fn set_sea_level_pressure(&self, pressure: f32);
    fn set_sea_level_temperature(&self, temperature: f32);
    fn get_pressure(&self) -> f32;
    fn get_temperature(&self) -> f32;
}
*/